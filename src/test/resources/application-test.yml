spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  jpa:
    hibernate.ddl-auto: update
    database: HSQL
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2console